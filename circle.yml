machine:
  java:
    version:
      oraclejdk8
  services:
    - docker

dependencies:
  pre:
    - make build

deployment:
  pipeline:
    branch: [master]
    commands:
      - make push

notify:
  webhooks:
    - url: https://deployer.mdl.cloud/trigger

